{
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "install": "cd FlickrSearch && yarn install && cd ../FlickrSearch.Api && dotnet restore",
    "dev-frontend": "cd FlickrSearch && yarn dev",
    "dev-server": "cd FlickrSearch.Api && dotnet watch run",
    "dev": "concurrently \"yarn dev-server\" \"yarn dev-frontend\"  --kill-others"
    },
  "devDependencies": {
    "concurrently": "6.0.0"
  }
}
